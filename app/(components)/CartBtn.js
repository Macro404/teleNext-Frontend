


export default function CartBtn ({ phone }) {

  const handleClick () => {
    const cart = document.getElementById('cart');
  }
  
  return (
    <button
    className="cart_button"
    onClick={handleClick}
    
    
    >🛒</button>
  )
}